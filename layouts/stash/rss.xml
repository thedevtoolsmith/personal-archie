{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
{{- $pages = $pctx.RegularPages }}
{{- else }}
{{- $pages = $pctx.Pages }}
{{- end }}
{{- $limit := .Site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
{{- $pages = $pages | first $limit }}
{{- end }}
{{- $author := or .Site.Params.author dict }}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    {{ with .Site.Language.Locale }}
    <language>{{.}}</language>
    {{end}}
    {{ with index $author "email" }}
    <managingEditor>{{.}}{{ with index $author "name" }}({{.}}){{end}}</managingEditor>
    {{end}}
    {{ with index $author "email" }}
    <webMaster>{{.}}{{ with index $author "name" }} ({{.}}){{end}}</webMaster>
    {{end}}
    {{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>
    {{end}}
    {{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{ end }}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with index $author "email" }}
      <author>{{.}}{{ with index $author "name" }} ({{.}}){{end}}</author>
      {{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ with .Description | safeHTML }}{{ . }}{{ else }}{{ .Summary | safeHTML }}{{ end -}}</description>
      <content:encoded>{{ (printf "<![CDATA[%s]]>" .Content) | safeHTML }}</content:encoded>
    </item>
    {{ end }}
  </channel>
</rss>
